# Keeps the SHA-pinned GitHub Actions current: Dependabot opens PRs that bump
# both the commit SHA and its `# vX.Y.Z` comment.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
